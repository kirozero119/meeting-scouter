# Contributing

会議スカウターへのコントリビューションを歓迎します。

## 開発手順

1. Forkしてブランチを作成します。
2. 変更を加えます。
3. `make validate && make test` を実行します。
4. 変更理由、テスト内容、出力例をPull Requestに記載します。

## 辞書への追加

`meeting-scouter/data/` の固定辞書には、次を満たす表現だけを追加してください。

- 複数の組織・会議で一般化できる。
- 単なる専門用語ではなく、会議の曖昧さや企業的な言い回しを検出する目的がある。
- 誤検出を増やす一般語ではない。
- `term` は表示用の正規名、`variants` は実際に数える表記揺れである。

文脈に依存する表現は固定辞書ではなく、`analysis-contract.md` の意味判定ルールを改善してください。

## スコア変更

スコア式の変更には、健康的な会議と空中戦会議の順序が維持されるテストを追加してください。既存スコアとの互換性を破る場合は、Pull Requestで明示してください。

## 行動規範

参加者や特定属性を攻撃する診断文は受け入れません。笑いの対象は人ではなく、会議の構造と表現です。
